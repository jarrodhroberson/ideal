nums => [1,2,3,4,5].
three => nums[2].
nums[4] => 'six'.

PI => 3.14159265359.

sqr(x) x * x.
z => sqr(x:3).

circumference(r) PI * sqr(x:r).

fib(x) fib(x:x-1) + fib(x:x-2).
fib(x:2) 1.
fib(x:1) 1.
fib(x:0) 0.

isEven(n) (n % 2) = 0.

Coordinate2D { x, y }

Coordinate3D { x, y, z }

Date
{
    y >= 0,
    m >= 1 & m <= 12,
    d isValidDay(d:d)
}

Time
{
    h >= 0 & h <= 24,
    m >= 0 & m <= 60,
    s >= 0 & s <= 60
}